#include "main.h"

/**
 * abs - cmputes the abs value  of an int
 * @n: the int in the question
 *
 * Return: the abs of the int.
 */
int_abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

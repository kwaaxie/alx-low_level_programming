#include "main.h"

/**
 * _isupper - checks for uppercase
 * @c: parameter
 r* Return :  0 if successfu
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 91)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
